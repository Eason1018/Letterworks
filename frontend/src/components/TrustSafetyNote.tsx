const TrustSafetyNote = () => {
  return (
    <aside className="trust-note">
      <p>
        Your draft only uses the facts shown above. Missing details are listed so you can
        decide what to add. Review the preview before you print, copy, or email.
      </p>
    </aside>
  );
};

export default TrustSafetyNote;
